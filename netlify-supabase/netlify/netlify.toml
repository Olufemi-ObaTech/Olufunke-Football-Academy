# ============================================================
# NOTE: This file is intentionally empty / for reference only.
# The ACTIVE netlify.toml is at: netlify-supabase/netlify.toml (project root)
# Netlify reads from the base directory root, NOT from this subfolder.
# Do NOT add build config here — it will be silently ignored.
# ============================================================
